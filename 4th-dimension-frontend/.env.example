NEXT_PUBLIC_API_URL=http://localhost:4000

# Server-side BFF routes call the NestJS backend at this URL.
# Ensure the backend is running on port 4000 (see backend .env).

# Frontend dev server (default in package.json scripts)
# PORT=3002
